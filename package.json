{
  "name": "todo-list",
  "version": "1.0.0",
  "description": "A simple To Do list implemented with typescript",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "lite-server --baseDir=app",
    "start": "concurrently \"npm run watch\" \"npm run server\"",
    "compile": "tsc",
    "watch": "tsc -w"
  },
  "author": "Danilo Marques Sá",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.3.0",
    "lite-server": "^2.6.1",
    "typescript": "^4.8.2"
  }
}
